name: CI

on:
  push:
    branches: [main, master]
    tags: ["v*"]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  validate:
    name: Validate Actions runner
    runs-on: ubuntu-latest
    steps:
      - name: Runner smoke
        run: echo "CI runner is available"
